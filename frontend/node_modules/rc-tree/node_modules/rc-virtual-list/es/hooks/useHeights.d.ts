import * as React from 'react';
import { GetKey } from '../interface';
export default function useHeights<T>(getKey: GetKey<T>, onItemAdd?: (item: T) => void, onItemRemove?: (item: T) => void): [(item: T) => (instance: HTMLElement) => void, () => void, Map<React.Key, number>, number];
